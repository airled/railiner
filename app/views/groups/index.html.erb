<h1>Список групп</h1>

<% @groups.each_slice(4) do |slice| %>
  <div class='row'>
    <% slice.each do |group| %>
      <div class='col-lg-3'>
        <%= link_to group.name_ru, group_categories_path(group.name) %>
      </div>
    <% end %>
  </div>
  <br>
<% end %>

