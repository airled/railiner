<h1>Список категорий</h1>

<% @products.each do |product| %>
  <div class="row">
    <div class="col-lg-4">
      <%= image_tag product.image_url %>
    </div>
    <div class="col-lg-8">
      <%= product.name %>
    </div>
  </div>
  <br>
<% end %>