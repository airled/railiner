<h1>Список категорий</h1>
<%= link_to 'Назад', root_path %>
<%= link_to 'Группы', groups_path %>
<%= link_to 'Продукты', products_path %>
<table>
  <tr>
<!--    <th>id</th>
    <th>Name</th> -->
    <th>Название</th>
    <th>Подробнее</th>
<!--    <th>Is new</th> -->
  </tr>

  <% @categories.each do |category| %>
    <tr>
<!--      <td><%= category.id %></td> -->
      <td><%= link_to category.name_ru, category.url %></td>
      <td><%= link_to 'Подробнее', category_path(category) %></td>
<!--      <td><%= category.name_ru %></td>
      <td><%= category.is_new %></td> -->
    </tr>
  <% end %>
</table>
